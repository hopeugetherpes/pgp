# PGP

A privacy-first OpenPGP workbench that runs entirely in the browser.

The complete source tree, standalone offline build, checksums, release provenance,
and automatic Vercel deployment configuration are published in this repository.
